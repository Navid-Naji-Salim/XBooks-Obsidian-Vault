
- has to have a backend
- has to have a frontend
- has to utilize these tools and frameworks: TypeScript, Express.js, HMTL, CSS, React
- has to be a node.js backend server running on the vite server dev environment
- has to be a CRUD/RESTful API
- has to be considered a simple accounting system that can do simple operations
- will take inspiration from the accounting concepts from zoho books
- will take inspiration from the way the UI is layed out in zoho books
- doesn't need to be too advanced or look like the next zoho books
- build the core accounting features using the concepts that you have learned, focus on a clean minimal system rather than a cluttered, complicated, intricate codebase that you do not fully understand or even worse, haven't built yourself
- after the core MVP and features that consider the application done in terms of it being functional are implemented, you can build on top of it using newly gained understandings of accounting concepts

## And just like this, our application can become a neat, highly organized, small in scale yet clean, usable application which portrays potential for being used and being eventually expanded upon.

[[Core of the Foundational Accounting Concepts That Will be Used]]